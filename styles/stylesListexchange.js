import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  wrapper: {
    marginHorizontal: 10,
  },

  viewTitle: {
    alignItems: "center",
  },
  title: {
    fontWeight: "bold",
    fontSize: 24,
  },
});

export default styles;
